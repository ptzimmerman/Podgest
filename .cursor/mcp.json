{
  "mcpServers": {
    "podgest": {
      "command": "/bin/bash",
      "args": [
        "-c",
        "export PATH=/Users/pete/.nvm/versions/node/v20.19.3/bin:$PATH && npx -y mcp-remote https://mcp.podgest.app/sse"
      ]
    }
  }
}
